'use client';

// BlackHoleOverlay is no longer needed since:
// - The 3D black hole mesh itself creates the visual transition
// - CameraRig handles auto-triggering the contact section
// Keeping as an empty component to avoid import errors.

export default function BlackHoleOverlay() {
  return null;
}
